<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Quotes</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="/css/tailwind.css">
  <style>
    .tab-border {
      border-bottom: 2px solid #8a8888;
    }

    .tab-active {
      border-bottom: 2px solid #7e22ce;
    }

    .tab:hover {
      color: #7e22ce;
    }

    .quote-container {
      border: 1px solid #e2e8f0;
      border-radius: 8px;
      padding: 16px;
      margin-bottom: 16px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .quote-content {
      max-width: 80%;
    }

    .quote-actions {
      display: flex;
      align-items: center;
    }

    .quote-actions button {
      background: none;
      border: none;
      cursor: pointer;
      margin-left: 8px;
    }

    .quote-actions button:hover {
      color: #7e22ce;
    }

    .quote-actions .fa {
      font-size: 18px;
    }
  </style>
</head>
<body>
  <%= let id = userId %>
  <div class="w-full m-auto flex flex-col items-center justify-center bg-gray-100">
    <div class="flex items-center mb-4">
      <h1 class="text-2xl font-bold mb-8 mt-6">My Quotes</h1>
      <div class="flex-grow border-t border-gray-200 ml-4"></div>
    </div>
    <div class="bg-white p-6 rounded-lg shadow-md w-full max-w-5xl ">
      <div class="flex space-x-4">
        <a class="tab tab-border tab-active px-4 py-2 mr-auto" href="#" onclick="addquotes(event, id)">Added Quotes <span class="bg-purple-200 text-purple-700 rounded-full px-2">4</span></a>
        <a class="tab tab-border px-4 py-2 m-auto" href="#">Liked Quotes <span class="bg-purple-200 text-purple-700 rounded-full px-2">1</span></a>
        <a class="tab tab-border px-4 py-2" href="#">Disliked Quotes <span class="bg-purple-200 text-purple-700 rounded-full px-2">1</span></a>
      </div>
    
      <div class="quote-content" id="quote_content">
        <p class="text-gray-800">Almost everything will work again if you unplug it for a few minutes, including you</p>
        <div class="text-right text-blue-600">- Anne Lamott</div>
      <div class="quote-actions">
        <button>
          <i class="fas fa-pencil-alt"></i>
        </button>
        <button>
          <i class="fas fa-trash-alt"></i>
        </button>
      </div>
    </div>
  </div>
  <script>

     function addquotes (event, userId) {
      console.log(userId);
      // await fetch(`/users/${userId}/quotes`)
      //   .then(response => response.json())
      //   .then(data => {
      //     // Clear existing quote content
      //     const quoteContent = document.getElementById('quote_content');
      //     quoteContent.innerHTML = '';

      //     // Loop through the fetched quotes and display them
      //     data.forEach(quote => {
      //       const quoteContainer = document.createElement('div');
      //       quoteContainer.classList.add('quote-container');

      //       const quoteText = document.createElement('p');
      //       quoteText.classList.add('text-gray-800');
      //       quoteText.textContent = quote.text;

      //       const quoteAuthor = document.createElement('div');
      //       quoteAuthor.classList.add('text-right', 'text-blue-600');
      //       quoteAuthor.textContent = `- ${quote.author}`;

      //       const quoteActions = document.createElement('div');
      //       quoteActions.classList.add('quote-actions');

      //       const editButton = document.createElement('button');
      //       editButton.innerHTML = '<i class="fas fa-pencil-alt"></i>';

      //       const deleteButton = document.createElement('button');
      //       deleteButton.innerHTML = '<i class="fas fa-trash-alt"></i>';

      //       quoteActions.appendChild(editButton);
      //       quoteActions.appendChild(deleteButton);

      //       quoteContainer.appendChild(quoteText);
      //       quoteContainer.appendChild(quoteAuthor);
      //       quoteContainer.appendChild(quoteActions);

      //       quoteContent.appendChild(quoteContainer);
      //     });
      //   })
      //   .catch(error => {
      //     console.error('Error fetching quotes:', error);
      //   });
    }

  </script>
</body>

</html>
